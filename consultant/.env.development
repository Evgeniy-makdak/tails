# Local desk (:5173) must use the same API as GitHub Pages / phone — Render Free.
# Override with consultant/.env.local if you need a local server on :8787.
VITE_API_URL=https://tailio-chat.onrender.com
VITE_WS_URL=wss://tailio-chat.onrender.com/ws
